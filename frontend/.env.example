# Base URL of the deployed API Gateway HTTP API, with no trailing slash.
# Leave unset (or copy this file without filling it in) to run against the
# offline fixture in src/data/sampleMovie.js.
#
# This is the ONLY value the browser needs. Never put AWS credentials here.
VITE_API_URL=
